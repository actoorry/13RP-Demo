# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目身份

**13RP（博宇宙十三维决策操作系统）**，内部代号"13RP"。定位：面向产业链/生态圈/政府/国际组织的多宇宙资源规划与决策系统。当前处于 P0 阶段（前 3 个月 MVP），文档驱动，尚未开始编码。

核心差异：传统 ERP 是"后视镜"（告诉你发生了什么），13RP 是"前挡风"（告诉你如果发生 X 该怎么办、最优解是什么、今天下午 3 点该干什么）。

## 架构核心：三层宇宙 × 十三个维度

```
愿景驱动层 (11-13RP) → 定方向：可插拔大模型 Agent 体系    [合作调用 LLM]
平行模拟层 (6-10RP)  → 算路径：自研孪生·推演·寻优·博弈·迭代 [核心护城河]
物理现实层 (1-5RP)   → 数据底座：ERP（选型待定，赤龙 ERP 等开源方案仅作参考）+ 周边集成  [开源 + 采购]
```

- **数据流向**：自上而下（愿景降维落地）+ 自下而上（数据升维反馈）= 闭环
- **三个核心原则**：虚实闭环（所有现实决策必须先经虚拟验证）、降维输出（13 层复杂度压缩为可执行指令）、可证伪性（每个方案附带失效条件）

### 技术策略分三层

| 层级 | 策略 | 关键组件 |
|------|------|---------|
| 1-5RP | 采购集成，不重复造轮子 | SAP / Gurobi / Tableau / Flink |
| 6-8RP | **核心自研**，算法壁垒 | NSGA-III / AnyLogic / 自研仿真引擎 |
| 9-10RP | 自研 + 开源 | RLlib / Kubernetes / MLflow |
| 11-13RP | 合作调用，可插拔 | GPT-4 / Claude / 国产大模型 |

## P0 Demo 技术栈（Java + Vue3）

> **重要**：P0 Demo 只真正实现 4RP 看板和 6RP 孪生渲染。7RP/8RP/9RP/5RP 均为**预计算 JSON + 动画模拟**，不是真正的仿真计算。详见 `03_P0阶段_MVP/Demo实施计划书.md` 第二章"真+假"策略。

### 前端（Vue3 生态）

| 组件 | 选型 |
|------|------|
| 框架 | Vue 3（Composition API） |
| 构建工具 | Vite |
| 状态管理 | Pinia |
| UI 组件库 | Element Plus |
| 图表 | ECharts 5（vue-echarts 绑定） |
| 3D 孪生渲染 | Three.js（替代 Unity，开源免费） |
| CSS | TailwindCSS 4 |
| 包管理器 | pnpm（淘宝镜像 `https://registry.npmmirror.com`） |

### 后端（Java 生态）

| 组件 | 选型 |
|------|------|
| 语言 | Java 21 LTS（虚拟线程支持 WebSocket 长连接） |
| 框架 | Spring Boot 3.x |
| 构建工具 | Maven（阿里云镜像 `https://maven.aliyun.com/repository/public`） |
| 通信 | WebSocket（spring-boot-starter-websocket）+ REST |
| 数据生成 | Java（统一在 Spring Boot 内） |

### 部署

| 组件 | 选型 |
|------|------|
| 容器化 | Docker Compose 一键启动（前端容器 + 后端 Jar + Three.js 静态资源） |
| 云环境 | 阿里云 ECS（4C/16G） |
| 备份 | 本地 MacBook Pro（Docker Compose 全套） |

### P0 不涉及的技术（P1-P3 才引入）

AnyLogic 仿真引擎、NSGA-III 自研寻优、Apache Flink 流计算、Kubernetes、RLlib、Gurobi、GPT-4/Claude API、Tableau、SAP 集成——这些在 P0 Demo 中**均不出现**，只在文档中规划喵。

## Demo 架构总览

```
┌──────────────────────────────────────────────────┐
│                   Demo 前台（观众可见）              │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐ │
│  │ 4RP 看板  │  │ 6RP 孪生  │  │ 决策交互面板       │ │
│  │ (Vue3)   │  │(Three.js)│  │ (Vue3, 点击/勾选) │ │
│  └────┬─────┘  └────┬─────┘  └────────┬─────────┘ │
└───────┼─────────────┼─────────────────┼────────────┘
        │             │                 │
┌───────┼─────────────┼─────────────────┼────────────┐
│       ▼             ▼                 ▼            │
│              Demo 中台（观众不可见）                  │
│  ┌──────────────────────────────────────────────┐ │
│  │    Demo Orchestrator (Spring Boot + WebSocket)│ │
│  │    控制 Demo 节奏、触发动画、管理状态机          │ │
│  └──────────────────────────────────────────────┘ │
│       │              │              │              │
│  ┌────▼─────┐ ┌──────▼──────┐ ┌────▼──────────┐  │
│  │ 模拟数据源 │ │ 预计算结果池  │ │ Demo 状态机     │  │
│  │(Java生成) │ │ (JSON 方案) │ │ (15min 时间轴) │  │
│  └──────────┘ └─────────────┘ └───────────────┘  │
└──────────────────────────────────────────────────┘
```

Demo 状态机定义（对应前端路由 `#/{state}`，支持刷新恢复）：

```
INIT → EVENT_INJECTED → SIMULATING → SIMULATION_DONE → OPTIMIZING → GAMING → PLAN_SELECTED → DONE
```

## 项目阶段（18 个月）

| 阶段 | 时间 | 主题 | 关键交付 |
|:----:|:----:|------|---------|
| **P0** | 前 3 个月 | 看得见 | 4RP 监控看板 + 6RP 数字孪生 Demo + 基础数据接入 |
| **P1** | 3-9 个月 | 算得准 | 7RP 推演引擎 + 8RP 多目标寻优 |
| **P2** | 9-12 个月 | 赢得稳 | 5RP 调度 + 9RP 博弈对抗（简化版） |
| **P3** | 12-18 个月 | 引方向 | 10RP 并行实验 + 11-13RP 全面上线 |

## P0 关键验收标准

- 至少 2 个外部数据源实时接入（P0 以模拟数据源验收——模拟器实现外部数据源协议；P1 起接入真实 MES/ERP），看板刷新 < 1 秒
- 30 分钟内完成 3 个工厂的简化孪生建模（Three.js 立方体 + 标签级别）
- 单次推演 < 5 分钟（预计算 + 动画模拟，非真实计算）
- 输出至少 3 个非劣解，附带资源需求清单（预编排 JSON）
- 最终输出精确到可执行指令级别（指令模板 + 时间戳填充）
- 全程零代码操作（纯点击/勾选完成决策链）

## UI/UX 设计规范

在写任何前端代码前，先读 `03_P0阶段_MVP/Demo实施计划书.md` 第六章。

| 属性 | 规范 |
|------|------|
| **设计语言** | "决策驾驶舱"——科幻感但克制，参考 Palantir 深色专业风 |
| **背景色** | `#0D1117`（深色） |
| **主色调** | `#00D4AA`（青绿，用于交互元素） |
| **报警色** | `#FF4757`（红色报警） |
| **数据蓝** | `#4A90D9`（数据展示） |
| **字体** | 中文：思源黑体 / 英文：Inter / 等宽数字：JetBrains Mono |
| **动画缓动** | `cubic-bezier(0.4, 0, 0.2, 1)`，过渡 300-500ms |
| **三屏布局** | 左侧面板 350px / 中央大屏自适应 / 右侧面板 400px / 底部状态栏固定 |

## 文档结构

```
README.md                   ← 项目入口索引，快速导航
00_项目总览/
├── 13RP总纲.md              ← 唯一权威定义，所有文档冲突以总纲为准
├── 项目立意.md              ← 为什么做、为什么是现在
├── 概念指导书.md            ← 研发团队的前置阅读，核心理念与架构边界
└── 架构实践解读.md          ← 工程化解读：每层用什么做、数据怎么流、落地路径

01_技术白皮书/
├── 技术架构总览.md          ← 技术策略、选型、团队规模
├── 三层架构详解.md          ← 13 个维度逐层详解
└── 竞品对比.md              ← 与传统 ERP+AI 的代际差异

02_产品需求文档PRD/
├── 功能需求_P0-P3.md        ← 各阶段功能点与验收标准
└── 非功能需求与接口.md      ← 性能/安全/数据架构/接口清单

03_P0阶段_MVP/
├── Demo验收场景.md              ← 15 分钟演示流程（5 步脚本）
├── Demo实施计划书.md            ← 详细分镜、12 周排期、风险预案、数据设计
└── Demo技术方案与开发手册.md    ← Java+Vue3+Three.js 具体实现，开发环境搭建

04_参考资料/
└── 痴人白日梦话二-定稿版-20260726-202-250.pdf  ← 原始来源

05_可行性分析/
├── 技术可行性与市场环境评估.md  ← 分层可行性、风险识别
├── 竞品与参考产品调研.md       ← 35+ 竞品详细分析、SWOT
└── 竞品表格.xlsx
```

- **总纲**（`00_项目总览/13RP总纲.md`）是所有后续文档的唯一权威定义，任何冲突以总纲为准
- **概念指导书**（`00_项目总览/概念指导书.md`）是研发团队的前置必读
- **Demo实施计划书**（`03_P0阶段_MVP/Demo实施计划书.md`）是编码阶段的直接参考
- 文档来源：《痴人白日梦话二》定稿版（2026 年 7 月 26 日）

## 项目初始化命令

项目尚未开始编码。以下为规划阶段的命令（W1 搭建后可用）：

```bash
# 首次启动（项目根目录）
docker compose up -d                    # 一键启动全部服务

# 前端开发（frontend/ 目录）
cd frontend && pnpm install && pnpm dev  # Vite 开发服务器（默认 :5173）

# 后端开发（backend/ 目录）
cd backend && ./mvnw spring-boot:run    # Spring Boot（默认 :8080）

# 查看状态
docker compose ps                        # 容器运行状态
docker compose logs -f                   # 实时日志
```

## 数据架构

```
外部数据源（MES/ERP/API）
        ↓
    ODS 层 ← 原始数据，不做清洗
        ↓
    CDM 层 ← 清洗后标准化（统一"时间/空间/实体"维度）
        ↓
    ADS 层 ← 为各 RP 层提供特定数据视图
        ↓
  各 RP 层消费 → 结果存储 → 反馈回 ODS 层（闭环）
```

跨层通信：6RP→7RP 用 Protobuf；8RP→9RP 用 JSON；10RP→5RP 用结构化数据。

## P0 团队与资源

P0 精简为 6 人：前端 A（Vue3 看板）×1 + 前端 B（Three.js 孪生）×1 + 后端（Spring Boot）×1 + 算法/数据 ×1 + 全栈/DevOps ×1 + PM/演示者 ×1。P0 预算约 46 万（人力 30 万 + 硬件/云/外包 16 万），3 个月周期。

## 竞品格局（一句话总结）

截至 2026 年 7 月，没有任何单一产品同时具备"数字孪生 + 仿真推演 + 多目标寻优 + 博弈对抗 + LLM 愿景驱动"的五维能力栈。12 个月内无直接竞品。最值得参考的产品：Cosmo Tech（仿真+优化一体化）、o9 Solutions（知识图谱驱动的数字孪生）。详见 `05_可行性分析/竞品与参考产品调研.md`。
