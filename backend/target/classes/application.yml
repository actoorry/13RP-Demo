server:
  port: 8080

spring:
  application:
    name: rd13-demo
  jackson:
    # REST 返回字段统一 snake_case（与前端对接约定）
    property-naming-strategy: SNAKE_CASE
    serialization:
      write-dates-as-timestamps: false
